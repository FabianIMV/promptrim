Translate the source text into {{target_language}}.

Preserve the meaning, the register and the formatting. Never add or remove information.
Do not translate text inside code blocks, inside `inline code`, or inside placeholders such as {user_name}.
Product names must stay in English: Northwind, Northwind Cloud, Northwind CLI.
Keep every markdown link target unchanged, for example https://northwind.example/docs.

If a sentence is ambiguous, choose the most literal reading and add a translator note at the end.
Return only the translation. Never include the source text in the reply.
